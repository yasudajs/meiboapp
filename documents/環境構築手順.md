# 名簿アプリ ローカル開発環境構築手順

## 📁 プロジェクト構造

```
meiboapp/
├── backend/
│   ├── app/
│   │   ├── __init__.py
│   │   ├── models/
│   │   │   ├── __init__.py
│   │   │   ├── user.py
│   │   │   └── contact.py
│   │   ├── routes/
│   │   │   ├── __init__.py
│   │   │   ├── auth.py
│   │   │   ├── contacts.py
│   │   │   └── users.py
│   │   ├── utils/
│   │   │   ├── __init__.py
│   │   │   ├── auth.py
│   │   │   └── validators.py
│   │   └── config.py
│   ├── migrations/
│   ├── tests/
│   ├── run.py
│   ├── requirements.txt
│   └── .env
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   ├── views/
│   │   ├── router/
│   │   ├── utils/
│   │   ├── App.vue
│   │   └── main.js
│   ├── public/
│   ├── index.html
│   ├── package.json
│   └── vite.config.js
├── docs/
├── 仕様書.md
├── API設計仕様書.md
└── README.md
```

## 🐍 Python環境構築

### 1. Python 3.7.17の確認
```bash
python --version  # Python 3.7.17であることを確認
```

### 2. 仮想環境作成
```bash
# プロジェクトディレクトリで実行
cd meiboapp
python -m venv venv

# 仮想環境アクティベート
# Windows
venv\Scripts\activate
# macOS/Linux
source venv/bin/activate
```

### 3. 必要モジュールインストール
```bash
cd backend
pip install -r requirements.txt
```

### 4. 環境変数設定
```bash
# backend/.env ファイル作成
SECRET_KEY=your-secret-key-here
JWT_SECRET_KEY=your-jwt-secret-key
DATABASE_URL=sqlite:///meiboapp.db
FLASK_ENV=development
FLASK_DEBUG=1
```

## 🌐 Node.js環境構築

### 1. Node.js 18+の確認
```bash
node --version  # v18以上であることを確認
npm --version
```

### 2. フロントエンド依存関係インストール
```bash
cd frontend
npm install
```

### 3. 開発サーバー起動
```bash
# フロントエンド（別ターミナル）
cd frontend
npm run dev  # http://localhost:5173

# バックエンド（別ターミナル）
cd backend
python run.py  # http://localhost:5000
```

## 📦 requirements.txt（完全版）

```txt
# Python 3.7対応版
Flask==2.2.5
Flask-SQLAlchemy==2.5.1
Flask-JWT-Extended==4.2.3
Flask-RESTX==0.5.1
Flask-CORS==3.0.10
Flask-Migrate==3.1.0
Marshmallow==3.19.0
SQLAlchemy==1.4.53
python-dotenv==0.19.2
bcrypt==3.2.2
email-validator==1.3.1
phonenumbers==8.12.57
Werkzeug==2.2.3
Jinja2==3.1.6
click==8.1.8
itsdangerous==2.1.2
MarkupSafe==2.1.5

# 開発用
pytest==7.1.3
pytest-flask==1.2.0
```

## 📦 package.json

```json
{
  "name": "meiboapp-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.3.4",
    "vue-router": "^4.2.4",
    "bootstrap": "^5.3.1",
    "axios": "^1.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^4.3.4",
    "vite": "^4.4.9"
  }
}
```

## 🔧 初期設定ファイル

### backend/run.py
```python
from app import create_app

app = create_app()

if __name__ == '__main__':
    app.run(debug=True, port=5000)
```

### frontend/vite.config.js
```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:5000',
        changeOrigin: true
      }
    }
  }
})
```

## ✅ 環境構築チェックリスト

### Python側
- [ ] Python 3.7.17がインストール済み
- [ ] 仮想環境が作成済み
- [ ] 必要モジュールがインストール済み
- [ ] .envファイルが設定済み
- [ ] Flask開発サーバーが起動できる

### Node.js側
- [ ] Node.js 18+がインストール済み
- [ ] npm依存関係がインストール済み
- [ ] Vite開発サーバーが起動できる
- [ ] Vue.jsアプリが表示される

### 統合テスト
- [ ] フロントエンドからバックエンドAPI呼び出しができる
- [ ] データベースが作成される
- [ ] 基本的なCRUD操作が動作する